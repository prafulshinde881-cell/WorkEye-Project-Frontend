services:
  - type: web
    name: work-eye-frontend
    env: static
    buildCommand: npm install; npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
